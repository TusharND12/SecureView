# 🚀 Run SecureView Now

## Quick Command

Open a terminal and run:

```bash
cd /home/aryan-budukh/Desktop/SecureVIew/SecureView
sudo apt update && sudo apt install -y openjdk-21-jdk && sudo update-alternatives --set java /usr/lib/jvm/java-21-openjdk-amd64/bin/java && ./run-secureview.sh
```

This will:
1. Install Java 21
2. Set it as default
3. Run SecureView

## Or Step by Step

```bash
# Step 1: Install Java 21
sudo apt update
sudo apt install -y openjdk-21-jdk

# Step 2: Set Java 21 as default
sudo update-alternatives --set java /usr/lib/jvm/java-21-openjdk-amd64/bin/java

# Step 3: Run SecureView
cd /home/aryan-budukh/Desktop/SecureVIew/SecureView
./run-secureview.sh
```

## What's Ready

✅ All code fixes applied  
✅ Cascade file path fixed  
✅ OpenCV paths configured  
✅ Project rebuilt  
⏳ Just need Java 21 installed

After Java 21 is installed, everything will work! 🎉

